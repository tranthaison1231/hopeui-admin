import ActivityOverview from "@/components/ActivityOverview"

const Support
    = (): JSX.Element => {
        return (
            <div className=" bg-[#E9ECEF] p-[100px]">
                <ActivityOverview percent={16} />
            </div>
        )
    }

export default Support
